/**
 * BLOCK: Count Down Timer - Save Block
 */

import classnames from 'classnames';

export default function save( props ) {
	const { className } = props;
	const {
		block_id,

	} = props.attributes;

	return (
		<div className={ classnames( className ) } >
		</div>
	);
}
